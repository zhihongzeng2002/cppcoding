#ifndef BITS_H
#define BITS_H

#include <string>

class Bits
{
public:
    Bits();

    static int mergeTwoNumbers(int n, int m, int i, int j);
    static std::string printBinary(double x);
};

#endif // BITS_H
